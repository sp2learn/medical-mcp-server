version: '3.8'

services:
  medical-web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MEDICAL_MODEL=gemini
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    env_file:
      - .env
    restart: unless-stopped